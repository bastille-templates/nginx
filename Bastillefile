# install required packages
PKG nginx

# copy files
CP usr /

# configure
CMD sed -i '' 's%server_name  localhost;%server_name  @${JAIL_IP};%g' /usr/local/etc/nginx/vhosts/defaults.conf

# enable and start
SYSRC nginx_enable=YES
SERVICE nginx start
